{
  "name": "secemp-blog-ui-tests",
  "private": true,
  "description": "Development-only DOM tests; the published Pelican site uses plain JavaScript.",
  "scripts": {
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@codemirror/state": "^6.7.5",
    "@codemirror/view": "^6.43.12",
    "js-yaml": "^4.3.2",
    "jsdom": "26.1.0"
  }
}
